package modeltests.model;

import static org.junit.Assert.*;

import org.junit.Test;

public class ShutDownThreadTest {

	@Test
	public void testRun() {
		/*
		 * Can't be tested since it just kills the program and saves static vars.
		 */
		assertTrue(true);
	}
}
